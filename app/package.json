{
  "name": "gptx-app",
  "version": "1.0.2",
  "description": "GPTx App",
  "main": "./out/main/index.js",
  "scripts": {
    "build": "tsc",
    "build:win": "npm run build && copy src\\assets\\logo.png out\\main\\logo.png && electron-builder --win --config --publish=never",
    "build:mac": "npm run build && cp src/assets/logo.png out/main/logo.png && electron-builder --mac --config --publish=never",
    "build:linux": "npm run build && cp src/assets/logo.png out/main/logo.png && electron-builder --linux --config --publish=never"
  },
  "author": "acrylic-style <me@acrylicstyle.xyz>",
  "license": "MIT",
  "devDependencies": {
    "electron": "^27.1.2",
    "electron-builder": "^24.9.1",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "@electron-toolkit/preload": "^2.0.0",
    "@electron-toolkit/utils": "^2.0.1"
  }
}
